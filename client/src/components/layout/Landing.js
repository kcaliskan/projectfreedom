import React, { Fragment } from "react";
import { Link } from "react-router-dom";

const Landing = () => {
  return (
    <Fragment>
      <Link to="/login">
        <div>hello</div>
      </Link>
    </Fragment>
  );
};

export default Landing;
